# Random-restart hillclimbing search for directed partial difference sets
To run, install [Rust](https://rust-lang.org/tools/install/) and build with 
```bash
cargo build --release
```
See `./target/release/rrhc --help` for help running.
